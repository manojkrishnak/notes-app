const NotFound = () => {
  const style = {
    height: "83vh",
    fontSize: "2rem",
    textAlign: "center",
    marginTop: "1rem",
  };
  return (
    <div style={style}>
      <h2>Page Not Found</h2>
    </div>
  );
};

export default NotFound;
